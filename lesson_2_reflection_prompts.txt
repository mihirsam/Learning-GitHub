What happens when you initialize a repository? Why do you need to do it?
ans : It will add a reflection directory in it which saves the metadata of repository.

How is the staging area different from the working directory and the repository?
What value do you think it offers?
ans : staging area is an intermidiate state where we add our files before commiting.
      Its make easier to commit one kind of changes. While working directory only saves
      the latest version of changes in files.

How can you use the staging area to make sure you have one commit per logical
change?
ans : we can put all the changes into staging area and commit when ready for final commit.


What are some situations when branches would be helpful in keeping your history
organized? How would branches help?
ans : Branches are helpful when we're trying to experiment with project which we
      are not sure to work.

How do the diagrams help you visualize the branch structure?
ans : It shows us how branches are connection to its parent commit and help us with merge.

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?
ans : 

What are the pros and cons of Git's automatic merging vs. always doing merges
manually?
